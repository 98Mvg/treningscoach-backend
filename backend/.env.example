# Backend Configuration
# Copy this file to .env and fill in your actual values

# ======================
# Claude API (Required)
# ======================
# Get your API key from: https://console.anthropic.com/
ANTHROPIC_API_KEY=sk-ant-your_api_key_here

# ======================
# Optional: OpenAI API
# ======================
# Only needed if you want to use OpenAI instead of Claude
# OPENAI_API_KEY=sk-your_openai_key_here

# ======================
# TTS Configuration
# ======================
# Qwen3-TTS runs locally on your server (no external API needed)
# Requirements:
#   1. Install: pip install torch torchaudio
#   2. Install Qwen3-TTS library when available
#   3. Place reference audio at: backend/voices/coach_voice.wav
#
# The app will automatically:
#   - Load reference audio on startup
#   - Clone voice once and cache it
#   - Generate speech locally (no API calls)
#   - Fall back to mock audio if TTS not available

# ======================
# Flask Settings
# ======================
# Flask environment (development/production)
FLASK_ENV=production

# Flask debug mode (0 or 1)
FLASK_DEBUG=0

# Port (default: 5000)
PORT=5000
